name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.13"
      - name: Sync and test
        run: |
          uv sync --group dev
          uv run pytest
          uv run ruff check src tests
          uv run black --check src tests examples --exclude examples/legacy
